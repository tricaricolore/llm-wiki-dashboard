# Karpathy Demo Vault

This directory contains the original sample vault shipped with the project.

It is kept as reference material only. The repository root is now a clean local
wiki vault with empty `raw/`, minimal `wiki/` pages, and empty report folders.

To inspect the demo, copy these folders back into a separate working tree or use
them as sample content for screenshots and experiments.

